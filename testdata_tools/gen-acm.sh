# This file provides support functions for generating testdata for ACM (i.e. non-scoring) problems.
# See generator_example_acm.sh for example usage.
USE_ACM=1
. "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/gen.sh"
